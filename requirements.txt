torch>=2.4
transformers>=4.50
trl>=0.12
peft>=0.13
accelerate>=1.0
bitsandbytes>=0.44
datasets>=3.0
pillow
sentencepiece
tqdm
wandb
# flash-attn is installed separately on the cluster:
#   pip install flash-attn --no-build-isolation
